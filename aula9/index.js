global = "Valor no escopo global";  // Declarar variáveis assim não é recomendado pois está no escopo global
console.log(global);

var nome = "David";
var nome = "Rodrigues";  // É possível redeclarar variáveis usando var (não recomendado) | Usando let isso não é possível
console.log(nome);